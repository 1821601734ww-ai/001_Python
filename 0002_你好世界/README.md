# 0002_你好世界

这是第二个 Python 入门练习目录。

接下来会在这里继续练习「你好，世界」和基础语法。

## 运行记录

- 在 PyCharm 中运行成功。
- 在 Cursor 中直接运行失败。

## 排查方向

- 可能原因：解释器或运行配置不一致（Python 路径、工作目录、环境变量不同）。
- 下一步：对比两边使用的 Python 解释器路径，并统一到项目 `.venv`。

## 处理结果（已解决）

- 已执行以下命令并创建项目虚拟环境：

```powershell
cd d:\002_Project\007_medical
py -3 -m venv .venv
```

- 结果：Cursor 中可以正常运行 Python 文件。
